# Alembic configuration. The database URL is not set here — env.py reads it
# from the app settings (DATABASE_URL env var), same as the service itself.
[alembic]
script_location = alembic
prepend_sys_path = .
